Changed mentions of "agents" to "adapters" in the `getting-started`, `guides`, and `index` narrative documentation to match a previous terminology rename.

List of pages touched and corrections made:
- `docs/getting-started/first-run.md`: Replaced "agents" with "adapters" where referencing CLI processes, updated `bernstein demo --real` command comment, fixed `No agents available` heading, and corrected `bernstein agents discover` drift by reverting to the still-existing valid command (while updating its comment to `rescan adapter-role catalogs`).
- `docs/getting-started/quickstart-tutorial.md`: Replaced "agents" with "adapters" to reflect CLI processes. Updated `bernstein agents showcase` command comment to properly reference adapters.
- `docs/getting-started/quickstart-demo.md`: Replaced references to "mock agents", "real agents", and "agent CLIs" with "mock adapters", "real adapters", and "adapter CLIs".
- `docs/getting-started/install.md`: Replaced mentions of "CLI coding agents" and "agent CLIs" with adapters, avoiding proper nouns like "OpenAI Agents SDK v2".
- `docs/getting-started/remote.md`: Fixed "agent CLI" / "agent task".
- `docs/guides/index.md`: Updated "CLI agents" description block.
- `docs/index.md`: Fixed "AI coding agents", "multi-agent runs", "mock agents", "Any agent", and "terminal dashboard: agents" across all introductory prose.
