## Fixes

- **Graduation:** Derives agent stage advancement decisions purely from the sealed event journal window and emits a cryptographically verifiable `GraduationReceipt`, ensuring graduation is an attestable evaluation rather than reliant on mutable counters.
