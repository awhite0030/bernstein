Ah! Wait, wait, wait!
The `quorum` and `PR policy` checks failed.
The issue with `PR policy` is: `BERNSTEIN_AUTOSYNC_TOKEN is required for autosync pushes; refusing to push with GITHUB_TOKEN`.
Let's see why this happened. Did my changes trigger autosync?
Did I run something that modified the source tree during the `determine-changes` phase and then `autosync` tried to push it back?
Wait, if `determine-changes` modified `.git/config`, `scripts/emit_affected.py` wrote `affected.json`.
Did I write `affected.json` to the repo directory, making the working tree dirty?
Yes! `affected.json` is generated by `scripts/emit_affected.py --output affected.json` and saved in the repository!
And `test` uses `affected.json`.
But wait! `test` and `determine-changes` are separate jobs! `test` downloads it.
If `determine-changes` leaves `affected.json` in the working tree, does it matter?
Wait, `determine-changes` runs `actions/checkout`.
Then `autosync` runs?
Let me check what `PR policy` job does!
